#include <bits/stdc++.h>
using namespace std;
ios::sync_with_stdio(0);
cin.tie(0);

freopen("input.txt", "r", stdin); 
freopen("output.txt", "w", stdout);

int main() {
    while (cin >> x) { //reads elements from input one after another until there is no more data available
            // code
    }
}
